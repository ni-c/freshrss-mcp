import{_t as e,it as t,n,rt as r}from"./chunks/framework.D9LADW4G.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1787052896000}`),a={name:`reference/changelog.md`};function o(n,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[t('<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-3-2-2026-09-07" tabindex="-1"><a href="https://github.com/ni-c/freshrss-mcp/releases/tag/v0.3.2" target="_blank" rel="noreferrer">0.3.2</a> - 2026-09-07 <a class="header-anchor" href="#_0-3-2-2026-09-07" aria-label="Permalink to “0.3.2 - 2026-09-07”">​</a></h2><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><p><strong>mcp-approval 0.8.2.</strong> 0.3.1 already shipped the single-use sealed dialog answer of 0.8.1, on npm and in the Docker image, which is built from the lockfile. 0.8.2 adds <code>orderedResourceKey</code>, taken up below.</p></li><li><p><strong>Approval keys bound to positions.</strong> <code>mark_all_as_read</code> keys its confirmation on the pair (stream, cut-off). <code>setResourceKey</code> sorts its parts before fingerprinting — set semantics — so a token issued for stream A and cut-off B would also have confirmed the pair the other way round. FreshRSS issues no stream id that is all digits, so the swap could not be expressed through the tool, but the shape is a tuple and the key now comes from <code>orderedResourceKey</code> in mcp-approval 0.8.2, which fingerprints each part at its position. <code>mark_articles</code>, <code>unsubscribe_feed</code>, <code>delete_category_or_label</code> and <code>import_opml</code> key sets or single ids and stay on <code>setResourceKey</code>.</p></li><li><p><strong>A refused login is not retried for ten seconds.</strong> Every tool call that needed a token was a fresh <code>ClientLogin</code>, and FreshRSS writes every refused one into its log as <code>Password API mismatch for user …</code>. <code>get_user_info</code> is annotated read-only, idempotent and cheap, and its 401 answer says &quot;check the password&quot; — which is what a model retries. A wrong password in the configuration was a burst of failed logins in the instance&#39;s log, and a reverse proxy&#39;s rate limit or a fail2ban jail keyed on that line locks the operator&#39;s own address out. A refused login (any status) is now remembered for ten seconds and repeated from memory, with a note saying so and when the next attempt is possible. The one retry after a 401 on a request is unchanged; its refusal is what starts the cooldown.</p></li><li><p><strong>The publish job installs without running install hooks.</strong> <code>release.yml</code>&#39;s publish job holds <code>id-token: write</code> for npm Trusted Publishing and ran a plain <code>npm ci</code>, so every dependency&#39;s install hook ran with the OIDC token reachable from the job environment. It runs <code>npm ci --ignore-scripts</code> like the audit job and the Dockerfile already did — nothing in the tree declares a hook — and <code>gh release create</code> checks the tag exists with <code>--verify-tag</code>.</p></li><li><p><strong>Tokens from the instance have a shape.</strong> The <code>Auth=</code> line of the login answer went into the <code>Authorization</code> header as it came; a carriage return or a NUL in it made undici throw <code>Headers.append: &quot;GoogleLogin auth=…&quot; is an invalid header value</code> — the instance&#39;s string, unlabelled and unbounded, into the model context. The write token rode in every form the same way. Both must be visible ASCII of at most 1024 characters; anything else is reported as &quot;without a usable Auth token&quot; / &quot;not a write token&quot;.</p></li><li><p><strong>The startup line does not print a value that is not a URL.</strong> A <code>FRESHRSS_URL</code> that failed to parse was printed (redacted for userinfo), and one with an unknown scheme was printed as <code>got ${scheme}</code> — a fifty-six-character key with a colon behind it is a valid URL whose scheme is the key. <code>FRESHRSS_API_PASSWORD</code> sits one line below this variable in every compose file, and a password pasted into the wrong line is exactly a value that does not parse. Only a value with <code>://</code> is quoted, redacted and cut to 120 characters; anything else is described by its length.</p></li><li><p><strong><code>mark_articles</code> validates the ids before it asks.</strong> The dialog was raised and the key built on the ids as written, and <code>assertArticleId</code> ran afterwards — so the person was asked about a list that might not run, and an approval could be spent on a call that then failed. Validation now comes first and the key is built from the validated ids; <code>articleIds</code> in the answer is the validated form.</p></li><li><p><strong>Control characters and lone surrogates in three more places.</strong><code>get_unread_counts</code> handed feed titles on as they came, unlike <code>list_feeds</code>; <code>export_opml</code> passed the document through with whatever was in it (XML 1.0 forbids those characters, so a well-formed export is unchanged); and <code>&amp;#xD800;</code> in an article — or a <code>\\ud800</code> escape in the instance&#39;s JSON — produced a lone surrogate, which <code>String.fromCodePoint</code> does not refuse and every UTF-8 encoder on the client side does. Every string that leaves this server is well-formed now (<code>toWellFormed</code>), and a surrogate reference decodes to U+FFFD.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p><strong>What the instance sends is read at a boundary, not through a cast.</strong> Every response was a TypeScript cast, and one value of the wrong shape took the whole listing down — as <code>Output validation error</code> from the SDK&#39;s check of the output schema, or as a <code>TypeError</code>. <code>feed/1e300</code> or a twenty-digit feed id (<code>.int()</code> refuses both); <code>1e999</code> in <code>unread-count</code>, which <code>JSON.parse</code> hands over as <code>Infinity</code>; <code>published</code> past ±8.64e12 seconds, which <code>toISOString</code> throws on; a number where a title, an author, a URL or an id belongs; <code>items</code> that is not a list; a body that is <code>null</code>. Objects, arrays, strings, finite numbers and safe integers are now decided at the boundary (<code>src/boundary.ts</code>), a field of the wrong shape is omitted and an element of the wrong shape is skipped, feed ids are bounded to fifteen digits, and display strings are cut at 2000 characters (URLs at 8192) so one oversized title cannot cost the listing. A property test feeds every read tool arbitrary JSON and shaped envelopes with arbitrary leaves through the whole server and asserts on the absence of those errors; the harness lists the tools once per connection, so every success path in the suite is checked against the listed schema.</p></li><li><p><strong>The status is decided before the body is read.</strong> A 401 or a 5xx with a body past the 64 MiB ceiling answered &quot;more than 67108864 bytes&quot; — the size instead of the status, no hint about the credentials, and the one retry a 401 earns never ran. An error body is now read under its own 64 KiB ceiling, which cuts rather than refuses.</p></li><li><p><strong><code>&amp;constructor;</code> in an article decoded to a function.</strong> The named-entity table was an object literal, so <code>&amp;constructor;</code> looked up <code>Object.prototype.constructor</code> and the replace callback wrote <code>function Object() { [native code] }</code> into the text. The table is a <code>Map</code>.</p></li><li><p><strong><code>import_opml</code> measures the document in the bytes FreshRSS reads.</strong> The ceiling was 900 000 characters on the document as given; FreshRSS reads 1 048 576 <em>bytes</em> and drops the rest, and the document sent is the rewritten one — canonical URLs and XML escaping make it longer. A document that would be cut on the FreshRSS side is refused before the dialog, with the size it would be sent at.</p></li><li><p><strong><code>import_opml</code> quotes FreshRSS&#39;s answer like every other tool</strong> — through <code>upstreamText</code>, cut and labelled, instead of the raw first 200 characters.</p></li><li><p><strong>Caller strings have ceilings at the schema.</strong> <code>since</code>, <code>until</code> and <code>older_than</code> (64 — <code>Date.parse</code> walks whatever it is given), <code>continuation</code> (256), article ids (64), category and label names (200), a feed URL (8192) and a title (1000).</p></li><li><p><strong><code>get_user_info</code> reads the account strings as strings</strong>, cleaned and cut to 200 characters; a number where the user name belongs no longer fails the schema.</p></li><li><p><strong>Trailing slashes in <code>FRESHRSS_URL</code></strong> are removed with a counted walk rather than <code>/\\/+$/</code>, which is quadratic in the length of the run.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The runtime image no longer carries yarn, corepack or <code>package-lock.json</code>; nothing in it runs them.</li><li><code>list_article_ids</code> carries <code>notes</code>, for the case where FreshRSS sends a continuation value larger than this server accepts.</li></ul><h2 id="_0-3-1-2026-09-06" tabindex="-1"><a href="https://github.com/ni-c/freshrss-mcp/releases/tag/v0.3.1" target="_blank" rel="noreferrer">0.3.1</a> - 2026-09-06 <a class="header-anchor" href="#_0-3-1-2026-09-06" aria-label="Permalink to “0.3.1 - 2026-09-06”">​</a></h2><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p><strong>Article text conversion was quadratic on nested angle brackets.</strong> The fixpoint loop that 0.3.0&#39;s successor on <code>main</code> (#28) introduced to strip markup until nothing changed peeled one <code>&lt;&gt;</code> per pass: a body of 120 000 characters of <code>&lt;</code> took 53 seconds per article, on the single thread that serves every other request, and any feed publisher could send it. Markup is now stripped in one counted pass, with a separator emitted behind a <code>&lt;</code> that is kept as text whenever the thing after it is dropped — so no deletion can assemble an element out of pieces that were not one, which is the property the loop existed for. The same input takes four milliseconds. The property test that found the original injection still holds; the escaped form of the new shape is pinned beside the other linear-time cases.</p></li><li><p><strong>An approval could be replayed for fifteen minutes.</strong> On protocol revision <code>2026-07-28</code>, which <code>serveStdio</code> negotiates since 0.3.0&#39;s follow-up, the sealed state travels through the client, and <code>mcp-approval</code> proved that an answer belonged to its question but not that it had not been used already. <code>mark_all_as_read</code> without <code>older_than</code> has the same resource key every time, so every replay landed. <code>mcp-approval</code> 0.8.1 spends each state on its first answer; <code>SECURITY.md</code> had described the gap as unreachable on the grounds of a transport this server no longer uses, and now describes what is enforced and what a restart still forgets.</p></li><li><p><strong>Response bodies from the instance are bounded.</strong> Every answer was read into memory whole; an instance — or whatever answers in its place under <code>FRESHRSS_INSECURE_TLS</code> — that never stopped sending had no ceiling. Bodies above 64 MiB are refused as they arrive, with the same &quot;narrow the request&quot; error the result ceiling gives. A declared length above it is refused before a byte is read.</p></li><li><p><strong>The write token was not refreshed on the 401 retry.</strong> After an API password change both cached tokens are stale; the retry re-logged in and then resent the form it was first handed, old <code>T</code> included, so the first write after a password change failed with a hint about wrong credentials. The form is now built per attempt.</p></li><li><p><strong>Article and enclosure URLs skipped the credential redaction</strong> that feed URLs get. A publisher who serves a paid feed with the credentials in its URLs tends to write the item links the same way; <code>url</code> and <code>enclosures[].url</code> are now redacted like <code>feedUrl</code>.</p></li><li><p><strong>Text the instance wrote reaches the model bounded and marked.</strong> The body of an upstream error, <code>quickadd</code>&#39;s <code>error</code>, an unexpected stream id and the answer <code>expectOk</code> quotes are cut at 200 characters (2 000 for an error body), stripped of control characters, and labelled as untrusted text from the instance. Titles, authors and feed names lose their control characters the way article bodies already did.</p></li><li><p><strong><code>import_opml</code> passed on an absolute URL it could not parse.</strong> A value that names a scheme or an authority and still does not parse — a space in the host, a port out of range — was treated like a relative one and left for FreshRSS&#39;s fetcher to read its own way. It is now refused as malformed; only a value with neither is left alone.</p></li><li><p><strong>Caller-supplied ids and lists are bounded.</strong> An article id is at most 20 decimal or 16 hexadecimal digits, which is what a 64-bit FreshRSS id is; <code>add_labels</code> and <code>remove_labels</code> take at most 50 names each.</p></li><li><p><strong><code>get_user_info</code> and <code>subscribe_feed</code> broke on a client that validates.</strong> Both answered through the marked result, which adds <code>untrusted</code> and <code>source</code> — two fields their closed output schemas do not name. A client that had loaded <code>tools/list</code> checked the answer against the schema and threw a <code>ProtocolError</code> on the success path of both; one that had not saw nothing wrong, which is why the suite did not either. Both are this server&#39;s own words and now answer as such, and a test loads the schemas before calling.</p></li><li><p><code>FRESHRSS_URL</code> is stored in its parsed form. A stray space around the value, a query or a fragment used to be glued in front of the API path; the origin and path are kept, the rest is dropped with a warning.</p></li><li><p>A bad <code>ELICITATION</code> value is echoed shortened and without control characters, in case what was set was a line pasted into the wrong variable.</p></li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p><code>mcp-publisher</code> in the release and registry workflows is pinned to a release and checked against its published checksum; the job holds an OIDC token, so what it runs has to be what was reviewed.</p></li><li><p>A dependency review runs on every pull request, failing on a high-severity advisory the change would introduce.</p></li><li><p>oxlint&#39;s <code>suspicious</code> category is on, and what it found is fixed: shadowed names in the article and feed tools, helpers scoped inside test suites, and <code>sort()</code> where <code>toSorted()</code> was meant. The build target moves to ES2023 for the latter, which every supported Node has.</p></li><li><p>The tool reference marks the <code>essential</code> preset and the tools that ask a person before they act, per tool rather than only in the introduction. A test keeps both sets in step with the code.</p></li><li><p><code>homepage</code> in <code>package.json</code> points at the documentation site rather than at the README anchor on GitHub. It is what npm shows next to the package, and every one of these servers has had a documentation site for weeks.</p></li></ul><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li>The server introduces itself in full. <code>title</code>, <code>description</code>, <code>websiteUrl</code> and <code>icons</code> now travel with <code>name</code> and <code>version</code>, so a client that shows a server to a person has something to show. All four were already in <code>server.json</code> for the registry and reached no client at all; a test compares the two so they cannot drift.</li><li>Server <code>instructions</code>. Results carry an <code>untrusted</code> marker, but that is read after the fact — this is the channel a model sees before it calls anything.</li><li>An OpenSSF Scorecard run, weekly and on every push to <code>main</code>, reporting into the Security tab next to CodeQL and Trivy. The badge is the second in the row.</li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li>Source maps are no longer published in the npm tarball. Node reads them only under <code>--enable-source-maps</code>, which nothing here sets, and the maps pointed at a <code>src/</code> this package does not ship — so a stack trace under that flag named a file nobody could open. <code>dist/**/*.js</code> is unchanged; the package is about a fifth smaller.</li></ul><h2 id="_0-3-0-2026-09-03" tabindex="-1">[0.3.0] - 2026-09-03 <a class="header-anchor" href="#_0-3-0-2026-09-03" aria-label="Permalink to “[0.3.0] - 2026-09-03”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result — which six of them made unavoidable, since they answered with a sentence. The sentence stays, in the text block.</p><p>Every tool that reports feed content carries <code>untrusted: true</code> and <code>source: &quot;freshrss&quot;</code> as fields. This server has always said so in <code>notes</code>, which is prose in a list: a client can read it and cannot check it. Eight tools are without the marker, because their answer is entirely this server&#39;s own words — ids it was given, a sentence built from the arguments, the account it authenticates as.</p></li><li><p>The four tools that need a confirmation now <strong>ask the user</strong>, on clients that can show a prompt: <code>unsubscribe_feed</code>, <code>mark_all_as_read</code>, <code>delete_category_or_label</code> and <code>import_opml</code>. The two-call <code>confirm_token</code> remains for clients that cannot, so nothing that works today stops working — but where a person can be asked, one is, instead of a token that only proves the same call was made twice.</p></li><li><p>Each of those prompts now says what will be lost, which three of the four never did: FreshRSS keeps no record of which articles were unread, a feed takes its stored articles with it, and a deleted category moves its feeds to the default rather than deleting them.</p></li><li><p><strong><code>mark_articles</code> now asks too — but only when it is about to mark something read.</strong> It carries <code>destructiveHint: true</code> and went through unannounced, and its own description claimed &quot;All changes are reversible by calling this tool again with the opposite value.&quot; Three of the four are. Which of those articles were unread is not, and FreshRSS keeps no record of it — the same reason <code>mark_all_as_read</code> is guarded, over a caller-named list instead of a whole stream.</p><p>Starring, unstarring, labelling and marking <em>unread</em> still go straight through. Asking about a star toggle as well would be how people learn to tick without reading. The approval is bound to the exact list of <code>article_ids</code>, so one obtained for three articles does not execute against thirty.</p></li><li><p><code>ELICITATION</code> switches the dialog off — <code>false</code> sends a client that could have been asked down the two-call-token path instead. For a scheduled job or a test harness, where a dialog is the wrong shape rather than an unwanted one.</p><p>It does <strong>not</strong> remove the guard: there is no setting in which a guarded call goes unannounced. Two deliberate rough edges come with it. The variable is <strong>not prefixed</strong>, so one <code>export ELICITATION=false</code> reaches every MCP server in the environment — which is why a server started with it off prints a line saying so, and why the fallback text names the server instead of blaming a client that was working fine. And a value that is neither <code>true</code> nor <code>false</code><strong>stops the server</strong>: it is the only variable here that defaults to <em>on</em>, so failing open on a typo would leave the dialog running while the operator believed it was off. It is read after <code>FRESHRSS_API_PASSWORD</code> is wiped from the environment, so that exit cannot leave the password behind.</p></li><li><p>A <code>docs/guide/approval.md</code> page.</p></li><li><p><code>SECURITY.md</code> states what an approval binds — a decision to a request, not a decision to a moment — why the freshness gap that leaves is unreachable on this server today, and what would have to be built on the day it starts speaking protocol revision <code>2026-07-28</code>.</p></li><li><p>The live suite now pins three refusals against a real FreshRSS as well as the happy paths: the SSRF guard on <code>subscribe_feed</code> and on <code>import_opml</code>, each asserted with its reason rather than with a bare &quot;this failed&quot;, and a read-only server registering no write tool.</p></li></ul><h3 id="changed-3" tabindex="-1">Changed <a class="header-anchor" href="#changed-3" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid spellings that are legal JSON Schema and still get a tool refused, or its constraint silently dropped, by some MCP clients: an open object now writes <code>&quot;additionalProperties&quot;: true</code> rather than the empty schema <code>{}</code> zod emits for it; and a value that was left untyped is declared as what it really is. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p><code>export_opml</code> answers <code>{opml}</code> instead of the document as the whole result. A schema whose root is a string is served to a 2025-era client rewritten as <code>{result: …}</code>, so the tool would have answered in two shapes depending on which revision the client spoke — and <code>truncated</code> now has somewhere to live.</p></li><li><p>A result too large even after article content is dropped is now an error. It used to answer with the JSON cut at the ceiling, which a text block tolerates and <code>structuredContent</code> cannot.</p></li><li><p>The two-call <code>confirm_token</code> prompt is an error result. What was asked for did not happen, which is what <code>isError</code> says. The text is unchanged and still carries the token.</p></li><li><p>A <code>confirm_token</code> that does not match its arguments is <strong>refused with the reason</strong> instead of being answered with a fresh prompt, and the confirmation prompt itself is a plain result rather than an error. Both are now the same in every server of the family.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it, and it is what lets the dialog above work on both protocol eras from one code path — including behind a stateless gateway, where the older mechanism silently fell back to the weaker token for every client.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the confirmation store, the host guard and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong>, <strong><code>mcp-internal-hosts</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 825 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p><p>The SSRF guard is the notable one: what leaves is the classification and the resolving, including the two rules this repository contributed upstream — a resolver answering <code>0.0.0.0</code> is declining rather than pointing at the fetching host, and a name that does not resolve is passed on. What stays is the refusal, in FreshRSS&#39;s own words. The behaviour is unchanged, and <code>import_opml</code> now stops resolving as soon as one host is refused instead of spending the whole ten-second budget to reach the same answer.</p></li><li><p>The shared libraries move to <code>mcp-approval</code> 0.7.1, <code>mcp-tool-allowlist</code> 0.2.1, <code>mcp-internal-hosts</code> 0.2.1, <code>mcp-integration-harness</code> 0.2.0 and <code>svg-asset-set</code> 0.2.0.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p><strong>A feed could stall the server with an article body that shows nothing.</strong> Markup was removed with <code>replace(/&lt;[^&gt;]+&gt;/g, &#39;&#39;)</code> inside a loop that repeated until the text stopped changing. Every <code>&lt;</code> with no <code>&gt;</code> behind it made <code>[^&gt;]+</code> run to the end of the input and then backtrack a character at a time, so an article could buy quadratic work with linear bytes — measured at 8.8 seconds per article for 122 048 bare <code>&lt;a</code>s, 40 seconds for a body of bare <code>&lt;</code>s, on the single thread that also serves every other request. The reachable form of it needs no invalid markup at all: <code>&amp;lt;a</code> repeated is escaped <em>text</em>, which nothing on the FreshRSS or SimplePie path has reason to touch, and the conversion decodes entities between its two passes.</p><p>The conversion is now a single left-to-right scan: at a <code>&lt;</code> it looks for the <code>&gt;</code> that closes it, discards the span, and never re-reads what it deleted. The repeat-until-stable loop is gone with it — a scan cannot splice <code>&lt;scr&lt;script&gt;</code> into a new tag, because it reads the fragment and the tag that follows it in one direction, which is what a browser&#39;s tokeniser does with the same bytes. The same payloads now take single-digit milliseconds.</p></li><li><p><strong>The response budget is charged for the markup read, not the text returned.</strong> This was the amplifier under the entry above: a body that strips away to nothing produced no output, so nothing was debited, the budget stayed whole, and every one of the up to 100 articles in the same <code>list_articles</code> response was handed a full slice again. An article that fills its slice now costs the per-article limit it was given, whatever came out — which is what the README claimed of the budget all along.</p></li><li><p><strong>A feed password containing an <code>@</code> was published in half.</strong> The redaction matched up to the <em>first</em> <code>@</code>, but userinfo ends at the last one before the path, and FreshRSS does not percent-encode the password it stores. A feed stored as <code>https://alice:p@ssw0rd@rss.example/feed</code> came back as <code>https://***@ssw0rd@rss.example/feed</code> from <code>list_feeds</code> and the OPML export — the exact disclosure the redaction exists to prevent. <code>https://host/users/@alice</code> is still left alone.</p></li><li><p><strong>A hostname can no longer crowd out the <code>import_opml</code> confirmation dialog.</strong> The hosts an OPML document points at were interpolated into the server&#39;s own question. <code>URL.hostname</code> has no length limit — IDNA is applied with <code>VerifyDnsLength=false</code>, so a single 5 000-character label parses and survives — so eight of them put tens of thousands of characters of readable, attacker-written text into the question, ahead of the consequence line, which every renderer would then push out of view. The question now states how many hosts there are; the names go on the caller-supplied line, where the approval library flattens and caps them.</p></li><li><p><strong><code>FRESHRSS_READ_ONLY</code> is read tolerantly, as a protection switch should be.</strong> It was compared with <code>=== &#39;true&#39;</code>, so <code>=1</code>, <code>=yes</code>, <code>=TRUE</code> or a trailing space started a server with every write tool registered and said nothing about it — the operator asked for the guard and had no way to learn they had not been given one. <code>1</code>, <code>true</code> and <code>yes</code> now all switch it on, in any casing, with surrounding whitespace ignored. <code>FRESHRSS_INSECURE_TLS</code> stays strict on purpose: that one <em>lifts</em> a protection, so a value nobody spelled exactly has to leave certificate validation on.</p></li><li><p>Confirmation tokens are compared with a <strong>constant-time</strong> comparison. The copy in this repository used <code>!==</code>, which leaks through timing how much of a guess was right. Reaching a token still requires having received it in a previous tool result, so this closes a margin rather than a hole.</p></li><li><p>An entry in <code>FRESHRSS_ALLOW_TOOLS</code> that is not tool-name-shaped is now <strong>redacted</strong> in the error rather than quoted back. <code>FRESHRSS_API_PASSWORD</code> and <code>FRESHRSS_ALLOW_TOOLS</code> are adjacent lines in every compose file, and a paste into the wrong one used to print the credential into the client&#39;s log.</p></li></ul><h2 id="_0-2-0-2026-08-27" tabindex="-1">[0.2.0] - 2026-08-27 <a class="header-anchor" href="#_0-2-0-2026-08-27" aria-label="Permalink to “[0.2.0] - 2026-08-27”">​</a></h2><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to “Added”">​</a></h3><ul><li><p><code>FRESHRSS_ALLOW_TOOLS</code> and <code>FRESHRSS_DENY_TOOLS</code> choose which of the 16 tools are registered. Both take comma-separated tool names or a prefix with a trailing <code>*</code>, the allow list decides what is in and the deny list is subtracted from it, and <code>FRESHRSS_ALLOW_TOOLS=essential</code> selects a curated seven — <code>list_feeds</code>, <code>list_categories</code>, <code>get_unread_counts</code>, <code>list_articles</code>, <code>get_articles</code>, <code>mark_articles</code>, <code>mark_all_as_read</code>. A model picks the right tool far more reliably from seven than from sixteen, and every visible tool costs context on every request. Nothing changes for an installation that sets neither.</p><p>A filtered tool is not registered at all, so it is absent from <code>tools/list</code> and answers <code>tools/call</code> with &quot;tool not found&quot; — the same cut <code>FRESHRSS_READ_ONLY</code> already makes, not a second, weaker one.</p><p>An entry that matches no tool <strong>stops the server at startup</strong>, naming the entry and listing the real names, rather than being ignored: an ignored typo leaves a tool missing from <code>tools/list</code> with nothing pointing at the cause.</p></li></ul><h3 id="changed-4" tabindex="-1">Changed <a class="header-anchor" href="#changed-4" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The README now carries the same eight badges, in the same order, as every other MCP server in this family, all of them reading from npm rather than hard-coded; the opening follows one shape; and the standalone &quot;Full documentation&quot; line is gone, because the docs badge three lines above it points at the same page.</li></ul><h3 id="fixed-3" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-3" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The container image no longer ships OpenSSL 3.5.7-r0, which carries <strong>CVE-2026-14456</strong> (denial of service via unbounded memory growth). The pinned <code>node:24-alpine</code> digest is already the newest one; Alpine&#39;s fixed 3.5.8-r0 has simply not been rebuilt into it yet, so the runtime stage now upgrades <code>libcrypto3</code> and <code>libssl3</code> by name. Upgrading those two rather than running a blanket <code>apk upgrade</code> keeps the rest of the image exactly as the digest pins it. The step can go once the base image ships the fix.</li></ul><h2 id="_0-1-6-2026-08-26" tabindex="-1">[0.1.6] - 2026-08-26 <a class="header-anchor" href="#_0-1-6-2026-08-26" aria-label="Permalink to “[0.1.6] - 2026-08-26”">​</a></h2><h3 id="fixed-4" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-4" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>A feed whose domain a resolver sinkholes is no longer refused. Every ad blocker and DNS filter answers <code>0.0.0.0</code> for a blocked name, and <code>0.0.0.0/8</code> classifies as loopback — so 0.1.5 turned &quot;your resolver blocks this domain&quot; into &quot;refusing to point FreshRSS at a loopback address&quot;, which was both wrong and unhelpful. A resolved unspecified address is now passed over; nothing can be fetched from it. <code>0.0.0.0</code> written into the URL itself is still refused, because that one does address the host.</li><li>An IPv6 scope id is stripped before the address is read. <code>net.isIP</code> accepts <code>::ffff:127.0.0.1%eth0</code>, which made the dotted-quad fold miss its anchor and the address come out as routable. A URL cannot carry one, but a resolver answer can.</li><li>A group of an IPv6 literal that is not hexadecimal is rejected outright rather than handed to <code>parseInt</code>, which stops at the first character it dislikes and returns a number for <code>7f00xyz</code> just as happily.</li></ul><h3 id="security-1" tabindex="-1">Security <a class="header-anchor" href="#security-1" aria-label="Permalink to “Security”">​</a></h3><ul><li>The metadata endpoints outside <code>169.254/16</code> are refused as well: <code>100.100.100.200</code> (Alibaba Cloud) and <code>192.0.0.192</code> (Oracle&#39;s legacy endpoint). Neither is link-local by address, so no range check reaches them, but both are the same thing by purpose.</li></ul><h2 id="_0-1-5-2026-08-25" tabindex="-1">[0.1.5] - 2026-08-25 <a class="header-anchor" href="#_0-1-5-2026-08-25" aria-label="Permalink to “[0.1.5] - 2026-08-25”">​</a></h2><h3 id="security-2" tabindex="-1">Security <a class="header-anchor" href="#security-2" aria-label="Permalink to “Security”">​</a></h3><ul><li><code>subscribe_feed</code> no longer accepts a loopback or link-local address written as an IPv4-mapped IPv6 literal. <code>URL</code> canonicalises <code>http://[::ffff:127.0.0.1]/</code> into <code>[::ffff:7f00:1]</code> and <code>http://[::ffff:169.254.169.254]/</code> into <code>[::ffff:a9fe:a9fe]</code> before the guard saw them, so the string comparison found nothing it recognised and approved both — while every dual-stack client dials them as <code>127.0.0.1</code> and the cloud metadata service. Addresses are now reduced to the IPv4 address they carry and compared numerically, which also covers the IPv4-compatible, IPv4-translated and NAT64 forms. <code>localhost.</code> with the root label got past the same comparison and is now read as <code>localhost</code>. (<a href="https://github.com/ni-c/freshrss-mcp/security/advisories/GHSA-qqh2-7466-82f8" target="_blank" rel="noreferrer">GHSA-qqh2-7466-82f8</a>)</li><li><code>import_opml</code> now checks the URLs in the document. <code>/subscription/import</code> makes FreshRSS subscribe to every <code>xmlUrl</code> and fetch it server-side — the same capability <code>subscribe_feed</code> guards, reached through a door that had no check on it at all. Every <code>xmlUrl</code> and <code>htmlUrl</code> is now held to the same rule, entity references are resolved first (as libxml does), and a scheme other than http/https is refused rather than left to FreshRSS to open. (<a href="https://github.com/ni-c/freshrss-mcp/security/advisories/GHSA-qqh2-7466-82f8" target="_blank" rel="noreferrer">GHSA-qqh2-7466-82f8</a>)</li><li>The attributes of an imported OPML document are read by walking it the way an XML parser does, not by searching for them with a regular expression. A regex pairs quotes by scanning raw text, so a literal <code>xmlUrl=&quot;</code> planted inside a single-quoted attribute value or inside a comment made it pair the wrong quotes: it would have read a harmless decoy host while libxml, which parses the document on the FreshRSS side, read the loopback URL next to it. A document that does not scan as well-formed XML is now refused rather than guessed at.</li><li>A hostname that is not a literal address is resolved and its addresses are checked, so a DNS record pointing at <code>127.0.0.1</code> or <code>169.254.169.254</code> no longer walks around the guard. A name that cannot be resolved here is still passed on: the FreshRSS server may sit in a different network with its own resolver.</li><li>An imported OPML document is now sent with its encoding declaration rewritten to UTF-8, which is what the body is actually encoded as. libxml believes the declaration over the bytes, so <code>&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-7&quot;?&gt;</code> made it read <code>+AHg-mlUrl=&quot;http://127.0.0.1/&quot;</code> as <code>xmlUrl=&quot;http://127.0.0.1/&quot;</code> — an attribute no check reading the document as text can see under that name. The same trick hid a <code>&lt;!DOCTYPE&gt;</code> from the declaration check, putting entity expansion and external entities back on the table.</li><li>The URLs that were checked are written back into the document before it is sent, the way <code>subscribe_feed</code> hands FreshRSS the parsed URL rather than the string it was given. Checking one document and forwarding another is what let <code>http://ok.example.com\\@127.0.0.1/feed</code> past: a URL parser reads its host as <code>ok.example.com</code>, the fetcher splits at the <code>@</code> and connects to <code>127.0.0.1</code>.</li><li>An <code>xmlUrl</code> behind a namespace prefix (<code>o:xmlUrl</code>) is matched on its local name, which is the name libxml reports it under.</li><li>The names of the cloud metadata service — <code>metadata.google.internal</code>, <code>instance-data</code> and their siblings — are refused by name. They resolve to <code>169.254.169.254</code> on the instance and to nothing anywhere else, so resolving them is exactly what cannot catch them.</li></ul><h3 id="changed-5" tabindex="-1">Changed <a class="header-anchor" href="#changed-5" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The <code>import_opml</code> confirmation prompt names the hosts the document would subscribe to instead of only counting its outline elements, and a document that will be refused no longer gets a confirmation token first.</li><li>An <code>xmlUrl</code> written as <code>//host/path</code> is read as the http URL it stands for, and a relative one is left to FreshRSS — both appear in real OPML exports and must not fail an otherwise valid import. <code>feed://</code> is refused, the way <code>subscribe_feed</code> already refused it: reading it as <code>http://</code> would quietly fetch over plaintext a feed that is served over https.</li></ul><h2 id="_0-1-4-2026-08-24" tabindex="-1">[0.1.4] - 2026-08-24 <a class="header-anchor" href="#_0-1-4-2026-08-24" aria-label="Permalink to “[0.1.4] - 2026-08-24”">​</a></h2><h3 id="fixed-5" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-5" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Article text no longer comes back with HTML in it. Entities were decoded after the tags had already been stripped, so any encoding of <code>&lt;script&gt;</code> — <code>&amp;lt;</code>, <code>&amp;#60;</code> or <code>&amp;#x3c;</code> — was rebuilt verbatim in the output, event handler attributes along with it. Markup is now removed again after decoding, repeatedly, until nothing changes.</li><li>A <code>&lt;script&gt;</code> whose closing tag falls outside the parsed slice no longer delivers its JavaScript as article text, and a tag cut in half by that slice no longer survives as a fragment.</li></ul><h2 id="_0-1-3-2026-08-18" tabindex="-1">[0.1.3] - 2026-08-18 <a class="header-anchor" href="#_0-1-3-2026-08-18" aria-label="Permalink to “[0.1.3] - 2026-08-18”">​</a></h2><h3 id="fixed-6" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-6" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><code>http://[::1]:…</code> no longer produces the &quot;plain http to a non-local host, the API password will be sent unencrypted&quot; warning. <code>URL.hostname</code> keeps the brackets around an IPv6 literal, so the loopback check never matched that notation.</li></ul><h2 id="_0-1-2-2026-08-18" tabindex="-1">[0.1.2] - 2026-08-18 <a class="header-anchor" href="#_0-1-2-2026-08-18" aria-label="Permalink to “[0.1.2] - 2026-08-18”">​</a></h2><h3 id="fixed-7" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-7" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The architecture diagram and the demo recording were not displayed at all — on GitHub or on npm. GitHub Pages had never issued the TLS certificate for freshrss-mcp.ni-c.de (<code>https_enforced</code> was <code>false</code>, the only repository where it was), so every image embedded from that domain was proxied by camo and answered with 502. The certificate has been reissued and HTTPS is enforced.</li><li>The architecture diagram no longer depends on the reader&#39;s operating system. It carried a <code>prefers-color-scheme</code> block, which resolves against the OS rather than the theme toggle of GitHub or npm — so dark-mode readers on a light OS got the light artwork on a dark page, and this diagram painted an opaque white rectangle over the full canvas, which is the worst case there. The README now uses <code>&lt;picture&gt;</code>, which is resolved against the page, and the <code>&lt;img&gt;</code> that npm falls back to brings its own card instead of a media query.</li><li>The documentation site declared no <code>og:image</code> at all, so links to it had no preview card anywhere.</li></ul><h3 id="changed-6" tabindex="-1">Changed <a class="header-anchor" href="#changed-6" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The diagram is generated from a single source, <code>docs/assets/architecture.source.svg</code>, by <code>npm run assets</code>. The rendered copies had already drifted apart; CI now fails if one of them is edited by hand.</li><li><code>docs/public/og.png</code> is generated at exactly 1280x640, GitHub&#39;s recommended size for a social preview.</li><li>The demo recording is shown on the documentation home page as well, not only in the README, and is pinned to the content column so its width no longer depends on what the vhs tape happened to record.</li><li>The TypeScript major is now parked in <code>.github/dependabot.yml</code> with its reason, instead of living only as an <code>@dependabot ignore</code> on the closed PR #1.</li></ul><h2 id="_0-1-1-2026-08-17" tabindex="-1">[0.1.1] - 2026-08-17 <a class="header-anchor" href="#_0-1-1-2026-08-17" aria-label="Permalink to “[0.1.1] - 2026-08-17”">​</a></h2><h3 id="changed-7" tabindex="-1">Changed <a class="header-anchor" href="#changed-7" aria-label="Permalink to “Changed”">​</a></h3><ul><li>First release published by CI, so this is the first version carrying npm provenance attestations, a GitHub release generated from this file, and an entry in the MCP Registry. 0.1.0 was published by hand to claim the package name and is functionally identical; prefer this version if you verify provenance.</li></ul><h2 id="_0-1-0-2026-08-17" tabindex="-1">[0.1.0] - 2026-08-17 <a class="header-anchor" href="#_0-1-0-2026-08-17" aria-label="Permalink to “[0.1.0] - 2026-08-17”">​</a></h2><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial release: MCP server for FreshRSS, speaking the Google Reader compatible API at <code>/api/greader.php</code>.</li><li>Read tools: <code>get_user_info</code>, <code>list_feeds</code>, <code>list_categories</code>, <code>get_unread_counts</code>, <code>list_articles</code>, <code>get_articles</code>, <code>list_article_ids</code>, <code>export_opml</code>.</li><li>Write tools: <code>mark_articles</code>, <code>mark_all_as_read</code>, <code>subscribe_feed</code>, <code>update_feed</code>, <code>unsubscribe_feed</code>, <code>rename_category_or_label</code>, <code>delete_category_or_label</code>, <code>import_opml</code>. Not registered when <code>FRESHRSS_READ_ONLY=true</code>.</li><li>Confirmation tokens for <code>mark_all_as_read</code>, <code>unsubscribe_feed</code>, <code>delete_category_or_label</code> and <code>import_opml</code>.</li><li>Plain-text conversion of article HTML with per-article and per-response size budgets.</li><li>Credentials embedded in feed URLs (<code>https://user:password@host/feed</code>, how FreshRSS stores HTTP-auth feeds) are redacted in <code>list_feeds</code> and <code>export_opml</code>.</li><li><code>subscribe_feed</code> refuses loopback and link-local targets: FreshRSS fetches the URL server-side, so the tool would otherwise be an SSRF primitive reachable from text inside an article. Private LAN addresses stay allowed.</li><li><code>import_opml</code> refuses documents with a <code>&lt;!DOCTYPE&gt;</code> or <code>&lt;!ENTITY&gt;</code> declaration, which is what carries entity-expansion and external-entity attacks into the FreshRSS server&#39;s XML parser.</li><li>Multi-architecture container image on <code>ghcr.io/ni-c/freshrss-mcp</code> with an SBOM and build provenance. npm is removed from the runtime image — it is unused there, and its vendored dependencies were the image&#39;s only HIGH/CRITICAL CVEs.</li><li>CI: lint, build and tests on Node 22 and 24, <code>npm audit</code>, CodeQL, and a Trivy scan of the image on amd64 and arm64. Releases publish to npm via Trusted Publishing with provenance and register with the MCP Registry.</li></ul>',58)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};